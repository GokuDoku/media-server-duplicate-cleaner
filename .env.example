# Media Duplicate Finder - Environment Configuration
# Copy this file to .env and modify the values as needed

# Docker Compose Configuration
# Path to your docker-compose.yml file
# The script will use this to automatically detect your media paths
DOCKER_COMPOSE_FILE=/path/to/your/docker-compose.yml

# Optional: Override Media Paths
# If you don't use docker-compose or want to specify paths manually
# Uncomment and set these values
#MEDIA_PATHS=/media/movies,/media/tv,/mnt/storage/media

# Optional: Log Level
# Set to DEBUG, INFO, WARNING, ERROR or CRITICAL
#LOG_LEVEL=INFO 